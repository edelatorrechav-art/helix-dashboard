Helix Customer Dashboard — a [Next.js](https://nextjs.org) app styled with Tailwind CSS.

The homepage reads customer data from `data/helix_customers.csv` and displays the total number of customers.

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to view it.
